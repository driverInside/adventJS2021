/**
 * countSheeps
 * @description Returns the red colored sheeps that contains n and a in their names
 * @param {Object[]} sheeps A list of sheeps
 * @param {string} sheeps[].name The sheep name
 * @param {string} sheeps[].color The sheep color
 * @returns {[{ name: string, color: string}]} Sheeps
 */
export default function countSheeps (sheeps) {
  return sheeps
}
